# Change Log — 2025-12-29

## Summary
Aligned remaining non-admin loading indicators to the shared loading components and button loading behavior.

## Changes
- Replaced referral page spinner with `PageLoading`.
- Standardized notification settings save spinner to `LoadingSpinner`.
- Updated review submit button to use shared `Button` loading state.
- Unified Web Push action buttons to show `LoadingSpinner` during requests.
- Replaced inline referral code placeholder with `InlineLoading`.

## Tests
- Not run (not requested).
