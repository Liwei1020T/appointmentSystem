/**
 * 积分历史页面路由 (Points History Page Route)
 * 路径: /points
 */

import PointsHistoryPage from '@/features/points/PointsHistoryPage';

export default function Points() {
  return <PointsHistoryPage />;
}
